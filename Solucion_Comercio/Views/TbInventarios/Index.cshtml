@model IEnumerable<Solucion_Comercio.Models.TbInventario>

@{
    ViewData["Title"] = "Inventario";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Inventario</h1>

@* <p>
    <a asp-action="Create" class="btn btn-success">Crear Inventario</a>
</p> *@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Existencia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdProductoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Existencia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdProductoNavigation.NombreProducto)
            </td>
            @* <td>
                    <a asp-action="Edit" class="btn btn-danger" asp-route-id="@item.IdInventario">Editar</a> |
                    <a asp-action="Details" class="btn btn-primary" asp-route-id="@item.IdInventario">Detalles</a> |
                    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.IdInventario">Eliminar</a>
            </td> *@
        </tr>
}
    </tbody>
</table>
